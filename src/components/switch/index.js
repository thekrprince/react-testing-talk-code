import React from "react";
import { Link } from "react-router-dom";

const Switch = () => {
  return (
    <Link to="/counter">
      <button>Click</button>
    </Link>
  );
};

export default Switch;
